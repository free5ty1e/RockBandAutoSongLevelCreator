click>=8.1.7
demucs>=4.0.0
librosa>=0.10.1
# basic-pitch is vendored as autorb/pitch (ONNX Runtime only, no TensorFlow)
# so we skip the tensorflow-macos dependency that breaks macOS arm64 + Python 3.12.
onnxruntime
mir-eval
resampy<0.4.3,>=0.2.2
whisperx>=3.1.1
mido>=1.3.0
pretty_midi>=0.2.10
construct>=2.10.69

# As of version 82.0.0 (released in early 2026), the setuptools maintainers finally pulled the plug and completely deleted pkg_resources from the package
setuptools>=68.0.0,<82.0.0

# Audio Codecs and Processing
torchcodec
# Pinned to avoid dependency issues 
torch<2.9.0
torchaudio<2.9.0
torchvision<0.24.0

soundfile
pydub

pyphen>=0.10

# Instrument transcription (new)
crepe>=0.0.12          # High-quality pitch detection
madmom>=0.16.1         # Drum onset detection, tempo
scikit-learn>=1.3.0    # For drum classification

# Neural drum transcription: ADTOF Frame-RNN (kick/snare/toms/hihat/cymbals).
# Not on PyPI — installed from source (needs git, torch, librosa, pretty_midi).
# Falls back to the librosa band-based detector+classifier when absent.
adtof-pytorch @ git+https://github.com/xavriley/ADTOF-pytorch.git


